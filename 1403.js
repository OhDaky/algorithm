function solution(n, arr) {
  let res = [];
  // 전체 스테이지 n
  // 실패율 = 스테이지에 도달. 클리어 하지 못함 / 스테이지 도달
  // 실패율 내림차순 스테이지 return

  const stages = arr.length;

  return res;
}

console.log(solution(5, [2, 1, 2, 6, 2, 4, 3, 3]));
